server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Additional settings for CORS, security headers, etc.
    location /api {
        proxy_pass http://backend_api;
    }
}
